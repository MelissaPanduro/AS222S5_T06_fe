version: '3'

services:
  app:
    image: melissapanduro/front_docker
    ports:
      - "4200:4200"
    environment:
      DATABASE_USERNAME: ${DOCKER_USERNAME}
      DATABASE_PASSWORD: ${DOCKER_PASSWORD}

      